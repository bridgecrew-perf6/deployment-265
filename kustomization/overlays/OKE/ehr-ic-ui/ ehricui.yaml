apiVersion: apps/v1
kind: Deployment
metadata:
  name: csi-ehr-ic-ui
  namespace: vidacom
spec:
  replicas: 1
  selector:
    matchLabels:
      app: csi-ehr-ic-ui
      tier: frontend
      track: stable
  template:
    metadata:
      labels:
        app: csi-ehr-ic-ui
        module: ehr
        tier: frontend
        track: stable
    spec:
      containers:
        - image: 'harbor.cs.local/vidaplus/ehr-ic-ui:${parameters.Tag}'
          imagePullPolicy: IfNotPresent
          name: csi-ehr-ic-ui
          ports:
            - containerPort: 80
              name: http
          resources:
            limits:
              memory: 1Gi
            requests:
              cpu: 25m
              memory: 256Mi
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: csi-ehr-ic-ui-service
  name: csi-ehr-ic-ui-service
  namespace: vidacom
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: csi-ehr-ic-ui
  type: ClusterIP
