apiVersion: apps/v1
kind: Deployment
metadata:
  name: csi-empi-ui
  namespace: vidacom
spec:
  replicas: 1
  selector:
    matchLabels:
      app: csi-empi-ui
      tier: frontend
      track: stable
  template:
    metadata:
      labels:
        app: csi-empi-ui
        module: empi
        tier: frontend
        track: stable
    spec:
      containers:
        - image: 'harbor.cs.local/vidaplus/empi-ui:${parameters.Tag}'
          imagePullPolicy: IfNotPresent
          name: csi-empi-ui
          ports:
            - containerPort: 80
              name: http
          resources:
            limits:
              memory: 1Gi
            requests:
              cpu: 25m
              memory: 256Mi
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: csi-empi-ui-service
  name: csi-empi-ui-service
  namespace: vidacom
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: csi-empi-ui
  type: ClusterIP
