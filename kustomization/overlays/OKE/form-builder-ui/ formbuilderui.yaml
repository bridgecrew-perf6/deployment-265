apiVersion: apps/v1
kind: Deployment
metadata:
  name: csi-form-builder-ui
  namespace: vidacom
spec:
  replicas: 1
  selector:
    matchLabels:
      app: csi-form-builder-ui
      tier: frontend
      track: stable
  template:
    metadata:
      labels:
        app: csi-form-builder-ui
        module: base
        tier: frontend
        track: stable
    spec:
      containers:
        - image: 'harbor.cs.local/vidaplus/form-builder-ui:${parameters.Tag}'
          imagePullPolicy: IfNotPresent
          name: csi-form-builder-ui
          ports:
            - containerPort: 80
              name: http
          resources:
            limits:
              memory: 1Gi
            requests:
              cpu: 25m
              memory: 256Mi
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: csi-form-builder-ui-service
  name: csi-form-builder-ui-service
  namespace: vidacom
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: csi-form-builder-ui
  type: ClusterIP
