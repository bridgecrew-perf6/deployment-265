apiVersion: apps/v1
kind: Deployment
metadata:
  name: csi-dms-ui
  namespace: vidacom
spec:
  replicas: 1
  selector:
    matchLabels:
      app: csi-dms-ui
      tier: frontend
      track: stable
  template:
    metadata:
      labels:
        app: csi-dms-ui
        module: dms
        tier: frontend
        track: stable
    spec:
      containers:
        - image: 'harbor.cs.local/vidaplus/dms-ui:${parameters.Tag}'
          imagePullPolicy: IfNotPresent
          name: csi-dms-ui
          ports:
            - containerPort: 80
              name: http
          resources:
            limits:
              memory: 1Gi
            requests:
              cpu: 25m
              memory: 256Mi
      imagePullSecrets:
        - name: regcred
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: csi-dms-ui-service
  name: csi-dms-ui-service
  namespace: vidacom
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: csi-dms-ui
  type: ClusterIP
