apiVersion: apps/v1
kind: Deployment
metadata:
  name: csi-ehr-er-ui
  namespace: vidacom
spec:
  replicas: 1
  selector:
    matchLabels:
      app: csi-ehr-er-ui
      tier: frontend
      track: stable
  template:
    metadata:
      labels:
        app: csi-ehr-er-ui
        module: er
        tier: frontend
        track: stable
    spec:
      containers:
        - image: harbor.cs.local/vidaplus/ehr-er-ui:4.1.4.2
          imagePullPolicy: IfNotPresent
          name: csi-ehr-er-ui
          ports:
            - containerPort: 80
              name: http
          resources:
            limits:
              memory: 1Gi
            requests:
              cpu: 25m
              memory: 256Mi
      imagePullSecrets:
        - name: regcred